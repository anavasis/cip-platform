# CIP Platform repository

This Git branch contains the **CIP Platform** application at repository root.

It is an independent product codebase. It must not be nested inside other
product repositories. Publish this tree to its own remote (for example
`anavasis/cip-platform`) as the canonical repository.
